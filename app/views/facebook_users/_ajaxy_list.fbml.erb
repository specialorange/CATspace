<ul>
<% for assignment in assignments %>
  <li>
	    <%= link_to assignment.title, :controller => "Assignments", :action => :show, :id => assignment.id %> (<%= link_to 'Edit', edit_assignment_path(assignment) %>) (<%= link_to 'Delete', assignment, :confirm => 'Are you sure?', :method => :destroy %>)
  </li>
<% end %>
</ul>